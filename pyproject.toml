[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flagsmith-ci"
version = "0.0.0"
description = "Continuous Integration tooling to integrate with Flagsmith."
requires-python = ">=3.14"
dependencies = [
    "requests~=2.32.5",  # HTTP client for Flagsmith API calls
]

[project.scripts]
collect-code-references = "code_references.collect:main"
upload-code-references = "code_references.upload:main"

[dependency-groups]
dev = [
    "pre-commit~=4.5.1",  # Git hooks for code quality checks
    "pytest~=9.0.2",  # Test framework
    "pytest-mock~=3.15.1",  # Mocking utilities for pytest
]

[tool.hatch.build.targets.wheel]
packages = ["src/code_references"]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "SIM",    # flake8-simplify
    "COM",    # flake8-commas (trailing commas)
    "RUF",    # Ruff-specific rules
]

[tool.pytest.ini_options]
testpaths = ["tests"]
